[
  // reparse a comment block's placeholders
  { "keys": ["alt+w"], "command": "bespoke_docs_reparse",
    "context": [
      { "key": "selector", "operator": "equal", "operand": "comment.block" }
    ]
  }
]
